# Feature backlog

Items are checked off as PRs merge. Each item has a brief description and a link or anchor to the full batch prompt (which lives in `prompts/`).

## Queued

- [ ] Live step previews — render JPEG preview frames from comfyws.ts as fading thumbnails during sampling. See `prompts/live-previews.md`.
- [ ] CheckpointConfig.baseModel UI — add a baseModel field to the Models tab Checkpoints sub-tab, populated from CivitAI metadata at ingest. See `prompts/checkpoint-basemodel.md`.
- [ ] Mask drawing canvas — replace external mask upload with a brush-based on-device masking UI in ReferencePanel. See `prompts/mask-drawing.md`.

## Done

(empty for now; populates as PRs merge)
